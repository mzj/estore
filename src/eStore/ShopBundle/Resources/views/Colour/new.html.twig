{% extends 'eStoreShopBundle::adminLayout.html.twig' %}

{% block container %}


    <form action="{{ path('eStoreShopBundleAdmin_colour_create') }}" method="post" {{ form_enctype(form) }}>
       
          
        {{ form_errors(form) }}
        <fieldset class="main">
            <legend>Colour create</legend>
            
            <fieldset class="nested"> 
                <legend>Basic data</legend>
                <ol>
                    <li>
                        {{ form_label(form.name) }}
                        {{ form_errors(form.name) }}
                        {{ form_widget(form.name) }}
                    </li>
                </ol>
            </fieldset>
        {{ form_rest(form) }}
        
        
        <p>
            <input type="submit" value="Add"/>
        </p> 
    </form>
   
{% endblock %}
