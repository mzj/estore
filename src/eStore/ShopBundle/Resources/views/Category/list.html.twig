{% extends 'eStoreShopBundle::adminLayout.html.twig' %}

{% block container %}

    <div id="categories">
        
        {% for category in categories %}
            {{ category.name }}
        {% else %}
            {{ categories|raw }}
        {% endfor %}
        
    </div>


{% endblock %}